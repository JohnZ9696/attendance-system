spring.application.name=attendance

# Supabase PostgreSQL (IPv4 pooler - direct db.<ref> host is IPv6-only)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
spring.datasource.username=${DB_USERNAME:postgres.dtdgwzvpquvpyttptlim}
spring.datasource.password=${DB_PASSWORD:NguyenMinhDuc9696}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080

# External APIs
fastapi.url=${FASTAPI_URL:http://localhost:8000}
fastapi.internal-api-key=${FASTAPI_INTERNAL_API_KEY:s5HpmgoZ4Wl5A9v8pJ6qLuAyWIrAZLU_nP3W3AeaUDc}

# Actuator
management.endpoints.web.exposure.include=health,info